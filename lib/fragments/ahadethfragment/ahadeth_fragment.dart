import 'package:flutter/material.dart';

class AhadethFragment extends StatelessWidget {
  const AhadethFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    
    );
  }
}